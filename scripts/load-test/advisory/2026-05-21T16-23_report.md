# Azure AI Search Load Test Report

**Generated:** 2026-05-21 16:23 UTC  
**Profiles tested:** hybrid, semantic, vector  
**Replicas:** 1  
**Concurrency:** 10 workers, 60s duration  

---

## Summary

| Profile | Replicas | p50 ms | p95 ms | p99 ms | QPS | 429 rate |
|---------|----------|--------|--------|--------|-----|----------|
| hybrid | 1 | 80 | 138 | 197 | 109.7 | 0.0% |
| semantic | 1 | 276 | 393 | 479 | 36.3 | 0.0% |
| vector | 1 | 66 | 113 | 182 | 130.8 | 0.0% |

---

## Semantic Ranker Impact

> **Why only hybrid vs semantic?**  
> `semantic` = `hybrid` + neural L2 re-ranker applied to the top-50 hybrid candidates.  
> Comparing these two isolates exactly what the ranker adds in latency and QPS cost.  
> `vector` uses different retrieval (no BM25 keyword) so it is not a meaningful baseline here.

| Profile | p50 ms | p95 ms | p99 ms | QPS | 429 rate |
|---------|--------|--------|--------|-----|----------|
| hybrid | 80 | 138 | 197 | 109.7 | 0.0% |
| semantic | 276 | 393 | 479 | 36.3 | 0.0% |

**Ranker p95 overhead @ 1 replica(s):** +255ms (+184%)  
**QPS impact:** 109.7 → 36.3 (-67%)  

_Higher latency reflects the re-ranking step, not replica saturation._

---

## Findings

- ✅ [HEALTHY]  hybrid profile @ 1 replica(s): p95=138ms, 429_rate=0.0% — within acceptable bounds at current concurrency.
- ℹ️ [SEMANTIC_OVERHEAD]  Semantic profile p95=393ms — expected neural re-ranking overhead (hybrid baseline is typically 3–4x faster). No throttling; service is healthy. This latency is the ranker cost, not replica saturation.
- ✅ [HEALTHY]  vector profile @ 1 replica(s): p95=113ms, 429_rate=0.0% — within acceptable bounds at current concurrency.

---

## Recommendations

_No scaling recommendations at current concurrency._

---

## Log Analytics

See [`kql/README.md`](kql/README.md) for queries to run in a Log Analytics workspace.

| Query file | Purpose |
|------------|---------|
| `kql/throttling.kql` | 429 rate by 5-minute window |
| `kql/latency.kql` | p50/p95/p99 latency trend |
| `kql/semantic-impact.kql` | Before/after latency when enabling semantic ranker |
